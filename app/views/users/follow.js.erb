function renderUser(userHTML) {
  const user = document.querySelector("[data-user='<%= @user.id %>']");
  user.innerHTML = userHTML;
}

renderUser('<%= link_to follow_user_path(@user.id), method: :post, remote: true, class: "card-add-link" do %><i class="fas fa-heart"></i><% end %>');
