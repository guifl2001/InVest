<div class="col-xs-4 text-align">
  <%= simple_form_for [ faculdade ,review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience", class: 'review_form' %>
    <%= f.submit "Send your review" %>
  <% end %>
</div>
